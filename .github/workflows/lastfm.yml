name: Last.fm Widget Updater

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
          
      - name: Install dependencies
        run: pip install requests
          
      - name: Generate and verify widget
        run: |
          # Генерируем SVG
          python scripts/generate_svg.py
          
          # Проверяем что файл создан
          if [ ! -f "assets/lastfm_widget.svg" ]; then
            echo "❌ Файл SVG не создан!"
            exit 1
          fi
          
          echo "=== Проверка содержимого SVG ==="
          head -n 3 assets/lastfm_widget.svg
        env:
          LASTFM_API_KEY: ${{ secrets.LASTFM_API_KEY }}
          LASTFM_USERNAME: ${{ secrets.LASTFM_USERNAME }}
          
      - name: Commit changes
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add assets/lastfm_widget.svg
          git commit -m "🔄 Авто-обновление виджета [skip ci]" || echo "Нет изменений"
          git push
